import mongoose from 'mongoose';
              var Schema = mongoose.Schema;
              const Test2Schema  = new Schema({})
                export default mongoose.model("Test2", Test2Schema, "Test2");