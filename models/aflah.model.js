import mongoose from 'mongoose';
              var Schema = mongoose.Schema;
              const aflahSchema  = new Schema({})
                export default mongoose.model("aflah", aflahSchema, "aflah");