import mongoose from 'mongoose';
              var Schema = mongoose.Schema;
              const TrackingsssSchema  = new Schema({})
                export default mongoose.model("Trackingsss", TrackingsssSchema, "Trackingsss");