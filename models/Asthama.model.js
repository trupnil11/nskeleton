import mongoose from 'mongoose';
              var Schema = mongoose.Schema;
              const AsthamaSchema  = new Schema({})
                export default mongoose.model("Asthama", AsthamaSchema, "Asthama");